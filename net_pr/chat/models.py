# from django.db import models
#
# from net_pr.net_app.models import User
#
#
# class ChatDialog(models.Model):
#     user_first = models.ForeignKey(User, on_delete=models.CASCADE, related_name='first_friend')
#     user_seco = models.ForeignKey(User, on_delete=models.CASCADE, related_name='first_friend')
